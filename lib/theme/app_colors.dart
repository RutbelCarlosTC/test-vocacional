import 'package:flutter/material.dart';

class AppColors {
  // Colores principales extraídos del logo de CEPRUNSA
  static const Color primaryBlue = Color(0xFF1A1F44); // Azul Marino profundo
  static const Color secondaryRed = Color(0xFF700D21); // Guinda / Rojo oscuro
  static const Color accentGray = Color(0xFFA7A9AC); // Gris del logo
  
  // Colores de soporte
  static const Color background = Color(0xFFF5F6F9);
  static const Color surface = Colors.white;
  static const Color error = Color(0xFFB00020);
  
  // Colores de texto
  static const Color textPrimary = Color(0xFF1A1F44);
  static const Color textSecondary = Color(0xFF636781);
  static const Color textOnPrimary = Colors.white;
}
